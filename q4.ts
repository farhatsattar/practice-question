// 4: Declare two variables, firstName and lastName, then create a third variable fullName that combines them. Log the result.
let firstName = ("farhat");
let lastName = ("zafar");
let  fullName = (`${firstName} ${ lastName}`); 
console.log(fullName);
