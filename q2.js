"use strict";
// 2 : Create a variable temperature and assign it a value. Then log "It's cold!" if the temperature is below 20 degrees Celsius.
let temp = 16;
if (temp < 20) {
    let today_Temp = ("its cold today!");
    console.log(today_Temp);
}
